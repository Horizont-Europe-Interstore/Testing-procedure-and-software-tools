# Ignore private certificates and keys used by nginx and app
/interstore/app/certs/cert_key.pem
/interstore/app/certs/*.key
# If you keep autogenerated server.crt and server.key files locally, ignore them too
/interstore/app/certs/server.key
/interstore/app/certs/server.crt

# Optional: ignore all pem files in certs if you don't want any certs in repo
#/interstore/app/certs/*.pem
docker-compose-enelxdev.yml
docker-compose-enelxprod.yml

# Certificate files - do not commit secrets
*.p12
*.jks
*.key
*.crt
*.pem