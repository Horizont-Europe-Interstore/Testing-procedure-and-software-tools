package interstore.Identity;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RespondableSubscribableIdentifiedObjectRepository extends JpaRepository<RespondableSubscribableIdentifiedObjectEntity, Long> {
}
